// Break it Down

// explicitly write out the steps you must take
// talk through each step as you  write it out

function charCount(str) {
  // create an object
  // loop over the string
  // if key exists and char is a num/letter, increment count
  // if key doesnt exist and char is a num/letter, add key and set count to 1
  // else, dont do anything
  // return the object
}
